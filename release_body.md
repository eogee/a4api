# a4api v0.3.3

## 修复

### 技能管理：目录链接（junction）skill 被误拒操作

- 部分用户把 skill 以**目录联接**（junction / 软链接）形式放进工具的全局目录（例如 Codex 端 `video-use` 是指向仓库真实目录的链接），点「预览 / 打开 / 删除」时弹出「该路径不在任何已知 skill 存放区中，拒绝操作」
- 原因：操作前的归属校验使用 `Path.resolve()`，会把 junction 解析到真实目标路径，与发现流程（不解析链接，链接就是一个普通子目录）口径不一致，导致路径被误判
- 现在归属校验改用**不解析链接、不访问文件系统**的纯字符串归一化，与发现流程同视角；junction skill 可正常预览 / 打开 / 删除，且删除只移走链接本身，链接指向的真实目录不受影响，回收站恢复后链接原样接回

### 技能管理：预览报「not valid JSON」的真实原因被掩盖

- 部分用户机器上存在 Windows 11 24H2 的「不受信任装入点」，归属校验中一处未加容错的 `resolve()` 会间歇性抛出 WinError 448，后端未捕获返回 500；前端又把「JSON 解析失败」当错误信息弹出（`Unexpected token 'I', "Internal S"... is not valid JSON`），真实原因被掩盖
- 归属校验链路已不再访问文件系统，从结构上消除 448 触发条件；前端统一响应解析，非 JSON 错误响应（如 500 纯文本）直接显示真实内容与 HTTP 状态码，不再出现令人困惑的 JSON 解析报错

## 改进

- **logo / favicon 资源更新**（页面缓存戳刷新至 20260925）

## 校验

- 安装包：`a4api-setup-0.3.3.exe`
- SHA256：`480104F1BD7EFF2F095D4BCFAC415394C03649CC54EC33D8CF4989622474A8E7`
