# a4api v0.2.0

## 更新内容

### 新功能：三端技能管理
- 新增「技能管理」页，自动发现 **Claude Code / Codex / dsh** 三端的全局与项目级 skill（`<name>/SKILL.md` 目录 bundle），按 frontmatter name 聚合去重并标注「已在 N 端存在」。
- **跨端迁移（非破坏复制）**：任选源副本与任意「工具 × 项目/全局」目标，源端始终保留；目标端已存在同名旧版时先移入回收站再写入，绝不静默覆盖。
- **一键适配三端**：项目视图内一键把该项目全部 skill 按缺失端补齐到 Claude / Codex / dsh 三端，弹窗预览迁移计划。
- 迁移执行期间显示**实时进度条**（按目标/skill 粒度推进）并临时锁定页面其他操作；单个失败不中断，结束后汇总提示（已迁移/跳过/旧版入回收站/失败）。
- **技能回收站**：删除的 skill 移入回收站，30 天内可恢复原位或彻底删除，过期惰性清理。
- 每次迁移写入迁移日志，可追溯来源与去向；支持配置项目根目录列表（每行一个，一级子目录视为项目）。

### 交互优化
- 回收站操作列固定宽度，「恢复」「彻底删除」按钮不再换行。
- 迁移弹窗复选框/单选选中色统一为深黑 `#1c1e21`，悬停无样式变化。

### 测试与文档
- 新增 `test_skill_manager.py` 回归测试（沙箱隔离环境变量，不触碰真实数据）与手动测试文档《手动测试-技能管理》。

## 校验

- 安装包：`a4api-setup-0.2.0.exe`
- **SHA256：`D09ABCE9A8F19C5E9AA9D61E48A4C1F2CC535251546D4D95E700A9A4EF3DB876`**
- 建议下载后核对校验值，确保文件完整未被篡改。
