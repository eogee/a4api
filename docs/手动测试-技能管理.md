# 技能管理 · 手动测试文档

> 适用范围：a4api §9「技能管理」（发现 / 迁移 / 回收站 / 打开 / 预览 / 迁移日志）
> 建议按顺序执行；每个用例前的 `[ ]` 可勾选记录进度。

---

## 0. 准备工作

### 0.1 选择测试模式

| 模式 | 说明 | 适用 |
|---|---|---|
| **A 沙箱模式（推荐）** | 用环境变量把三端 skill 根、数据目录、项目根全部指向临时目录，**所有写操作都不碰真实数据** | 全部用例 |
| **B 真实数据模式** | 直接连真实 `~/.claude` 等，只做只读检查 + 少量可恢复的写操作 | 快速冒烟 |

### 0.2 启动（模式 A · 沙箱）

PowerShell 中执行（先改 `$T` 为你想用的临时目录）：

```powershell
$T = "C:\Temp\skill-test"          # 临时根，随意
# 准备沙箱目录与假 skill
"$T\claude-skills", "$T\codex-skills\.system", "$T\dsh-skills",
"$T\projects\proj-a\.claude\skills", "$T\projects\proj-a\.codex\skills",
"$T\projects\proj-b\.claude\skills" | ForEach-Object { New-Item -ItemType Directory -Force $_ | Out-Null }

function New-Skill($dir, $name, $desc) {
  New-Item -ItemType Directory -Force $dir | Out-Null
  "---
name: $name
description: ""$desc""
---

# $name
正文内容（Markdown）" | Set-Content "$dir\SKILL.md" -Encoding UTF8
}

New-Skill "$T\claude-skills\git-commit"   "git-commit"   "沙箱·提交技能"
New-Skill "$T\dsh-skills\git-dsh"         "git-commit"   "沙箱·提交技能(dsh副本)"
New-Skill "$T\codex-skills\.system\x"     "builtin-x"    "内置技能应被忽略"
New-Skill "$T\projects\proj-a\.claude\skills\deploy"   "deploy"   "沙箱·部署"
New-Skill "$T\projects\proj-a\.dsh\skills\deploy2"     "deploy"   "沙箱·部署(dsh副本)"

# 项目根列表写入沙箱数据目录
New-Item -ItemType Directory -Force "$T\data" | Out-Null
@{ roots = @("$T\projects") } | ConvertTo-Json | Set-Content "$T\data\projects.json" -Encoding UTF8

# 启动（端口如占用请更换）
$env:A4API_DATA_DIR            = "$T\data"
$env:A4API_CLAUDE_SKILLS_PATH  = "$T\claude-skills"
$env:A4API_CODEX_SKILLS_PATH   = "$T\codex-skills"
$env:A4API_DSH_SKILLS_PATH     = "$T\dsh-skills"
uv run python -m uvicorn backend.app.main:app --port 18923
```

浏览器打开 **http://127.0.0.1:18923**，切到「技能管理」tab。

> 注意：`$env:` 设置只在当前 PowerShell 会话生效，重开窗口需重新设置。
> 沙箱数据库位于 `$T\data\a4api.db`。

### 0.3 启动（模式 B · 真实数据）

```powershell
uv run python -m uvicorn backend.app.main:app --port 18923
```

真实模式下你机器上已有的数据（供核对发现结果）：
- 全局：三个根当前为空 → 全局视图应为空态
- 项目 `a4api`：git-commit / impeccable / release（仅 Claude 端）
- 项目 `AI-ML系列教程` 等存在大量 claude+dsh 重复项

---

## 1. 发现场景

### TC-01 tab 加载与全局视图
- [ ] 点击第三个 tab「技能管理」，出现「全局视图 / 项目视图」切换、右侧「项目根目录 / 迁移日志 / 回收站 / 刷新」
- [ ] 全局视图列出聚合卡片；`git-commit` 显示 **Claude、dsh 两枚端徽章 + 紫色「2 端存在」徽章**
- [ ] Codex 的内置目录 `.system/x` **未出现**（保留目录被跳过）
- [ ] 卡片上悬停某个副本行时出现「打开 / 删除」小链接

### TC-02 项目视图分组
- [ ] 点「项目视图」：按项目分块展示，每块有项目名、根路径、「一键适配三端」按钮
- [ ] `proj-b`（无任何 skill）**不出现**；`proj-a` 内 `deploy` 显示「2 端存在」

### TC-03 刷新
- [ ] 手动在沙箱 claude 根新建一个 skill 后点「刷新」，新卡片即时出现

---

## 2. 项目根目录配置

### TC-04 打开与回显
- [ ] 点「项目根目录」，文本框内为一行一个路径，当前值是 `$T\projects`

### TC-05 合法保存
- [ ] 新增一行 `$T\projects2`（先手动创建该目录），保存提示成功；项目视图随之多出该项目块（若其中放了 skill）

### TC-06 非法输入拒绝
- [ ] 只填相对路径 `abc` 保存 → 报错「必须是绝对路径」
- [ ] 填不存在路径 `C:\nope` 保存 → 报错「不存在或不是目录」
- [ ] 清空全部保存 → 报错「至少需要一个项目根目录」，且原配置未被破坏

---

## 3. 预览与打开

### TC-07 SKILL.md 预览
- [ ] 任一卡片点「预览」：顶部灰框显示描述与路径，下方 Markdown 正文正常渲染（标题/代码样式）
- [ ] 无正文的 SKILL.md 显示「SKILL.md 没有正文内容」占位

### TC-08 资源管理器打开
- [ ] 副本行悬停点「打开」：弹出资源管理器并定位到该 skill 目录

---

## 4. 迁移（核心）

> 复制语义：**源永远保留**。每次迁移后先确认源还在。

### TC-09 项目内跨端迁移（指定工具）
- [ ] 在 `proj-a` 视图对 `deploy` 点「迁移…」
- [ ] 弹窗上半部分可选拥有的源副本（Claude）；下半部分目标区含「全局 + 各项目 × 三端」
- [ ] 仅勾选 `proj-a → Codex`，执行后提示「已迁移 1 处」
- [ ] 文件管理器确认 `$T\projects\proj-a\.codex\skills\deploy\SKILL.md` 出现，`.claude` 下原件未动
- [ ] 刷新后该卡片变为「3 端存在」

### TC-10 一源多目标
- [ ] 对 `deploy` 再次迁移，同时勾选 `全局→Claude` 与 `proj-a→Codex` 以外的任意两个位置
- [ ] 提示「已迁移 2 处」；两处目录均出现副本

### TC-11 源位置禁用
- [ ] 迁移弹窗中与所选源完全一致的位置复选框为禁用态；切换上方单选换源后，禁用位置跟随变化

### TC-12 同名冲突保护
- [ ] 手动修改 `$T\projects\proj-a\.codex\skills\deploy\SKILL.md` 内容为「旧版内容」
- [ ] 从 Claude 端向 proj-a 的 Codex 再迁移一次 `deploy`
- [ ] 结果提示含「旧版入回收站 1」；新内容为「沙箱·部署」版本；打开「回收站」能看到原 Codex 版条目（原位置列正确）

### TC-13 项目 → 全局 / 全局 → 项目（跨上下文）
- [ ] 把 `deploy` 迁移一份到「全局 · dsh」：`$T\dsh-skills\deploy` 出现
- [ ] 全局视图选中 `deploy`，再迁移到 `proj-b → Claude`：proj-b 出现该 skill，proj-b 开始出现在项目视图

### TC-14 一键适配三端
- [ ] 选一个缺端的项目（如新建 `proj-c` 只放 Claude 端 skill），点其「一键适配三端」
- [ ] 先弹计划清单（哪个 skill → 缺哪几端），执行后提示「适配完成」
- [ ] 该项目内所有 skill 三端齐全；已齐全的项目点击则提示「已在三端齐全」

---

## 5. 回收站

### TC-15 删除 → 恢复往返
- [ ] 对任一多余副本点副本行「删除」→ 确认；卡片徽章数减少
- [ ] 「回收站」中可见条目：名称、原位置（全局/项目 · 端）、剩余天数 ≈30
- [ ] 点「恢复」→ 提示已恢复；文件回到原位置，回收站清空该项

### TC-16 恢复冲突拦截
- [ ] 删除某 skill 后，**手动在原位置新建同名目录**；再点恢复
- [ ] 应报错「原位置已存在同名目录」，且手动目录未被覆盖

### TC-17 彻底删除
- [ ] 回收站中对一条目点「彻底删除」→ 条目消失，磁盘上回收站目录同步消失

### TC-18 过期清理（30 天）
- [ ] 先制造一条回收站条目，然后停掉服务，把它的时间改成 31 天前：

```powershell
uv run python -c "
import sqlite3, pathlib, os
db = os.environ.get('A4API_DATA_DIR', r'backend\database') + r'\a4api.db'
con = sqlite3.connect(db)
con.execute(\"UPDATE skill_trash SET trash_time = datetime('now','localtime','-31 days')\")
con.commit(); print('updated:', con.total_changes)"
```

- [ ] 重启服务打开「回收站」：顶部提示「已自动清理 1 条超过 30 天的过期条目」，过期条目消失、其余保留

---

## 6. 迁移日志与安全

### TC-19 迁移日志
- [ ] 点「迁移日志」：此前每次成功/失败都有记录（时间、Skill、源、目标、结果）
- [ ] 人为制造一次失败（如把目标项目目录改名后再迁移到它）→ 日志中出现红色失败明细

### TC-20 越界路径防护（API 直调）
```powershell
Invoke-RestMethod http://127.0.0.1:18923/api/v1/skills/open -Method Post `
  -Body '{"path":"C:\\Windows\\System32"}' -ContentType 'application/json'
```
- [ ] 返回 HTTP 400（「不在任何已知 skill 存放区」），不会打开资源管理器

---

## 7. 收尾

- [ ] 关闭服务（Ctrl+C）
- [ ] 模式 A：整个 `$T` 目录可直接删除，真实数据零影响
- [ ] 模式 B：核对「回收站」为空或仅剩有意保留项；迁移日志中的演练记录可视需要忽略

## 已知边界（非 bug）

1. 只扫描主根：`<proj>/.agents/skills`、`~/.agents/skills` 次要根一期不做
2. Codex 全局 `.system/` 及一切点开头目录视为系统保留，不出现在列表
3. 「同名」判定 = frontmatter `name` 相同**或**目录名相同，二者任一命中即触发冲突保护
4. 项目视图只收录至少含 1 个 skill 的项目
